# 导航数据转换项目协作规则

## 重要原则

1. 所有面向用户的信息必须使用中文。
2. 每次代码或文档改动后提交相应的 Git，并推送到 GitHub。数据库、备份、日志、反编译结果、测试输出和外部测试包不得提交。
3. 网络访问失败时，提示用户检查网络或使用其自行配置的代理；不得在源码、界面、日志或文档中固化私人代理地址。
4. 覆盖游戏文件前必须确认游戏完全关闭，并为数据库及其元数据创建带时间戳的备份。
5. 未经实机验证的转换结果只能标记为测试版，不得创建正式 Release。

## 项目方向

- 当前仓库：`fenix_to_ini_builds`，GitHub：`https://github.com/JCH2333/fenix_to_ini`。
- 后续需要开发 Fenix 到 TFDI、FSL/FSLabs 等格式的转换器。
- 长期目标是以 `2607` 原始数据为统一来源，为 Fenix、iniBuilds、Aerosoft/ToLiss、TFDI、FSL/FSLabs、PMDG、iFly 等机模生成数据。
- 应建立“统一中间模型 + 每机模独立适配器”，不要把不同机模的字段规则堆进一个通用写表函数。

## 数据源与模板

1. `2607` 原始文件是导航内容来源；机模官方数据库是目标 schema 和运行时契约的模板。两者职责不能混用。
2. 开始支持新机模前，必须保存一份可正常使用的官方数据库，并记录：
   - SQLite schema、列顺序、类型、默认值和 NOT NULL 约束。
   - 索引、触发器、视图、外键、`user_version`、journal mode。
   - Header、AIRAC 周期、Revision 及外部元数据文件。
   - 官方数据中的取值范围、空值语义、单位和物理行顺序。
3. “表结构几乎相同”不代表加载契约相同。iniBuilds A340 与 AS346 就存在相同 DFDv2 表、不同 WASM 行为的情况。
4. 成熟参考工具和参考数据库只能用于确认映射与行为；实现必须保留来源边界，不把第三方数据库、可执行文件或专有产物提交到仓库。

## 先确认真实加载契约

1. 不要只根据 schema 猜测。优先从机模 WASM、DLL、日志或可执行文件中提取真实 SQL、错误文本和加载顺序。
2. 对每条真实 SQL 建立本地模拟器，使用与机模完全相同的过滤、排序和游标推进方式验证输出。
3. SQL 没有 `ORDER BY` 时也要检查物理行顺序。加载器可能错误地假定 SQLite 按标识符排序。
4. 固定内存加载器需要额外检查：字符串最大长度、NULL、枚举值、负数、异常距离、容量上限和数组索引范围。
5. Windows 的 `0xc0000005` 或退出飞行后崩溃，可能是 WASM 更早发生内存破坏，不能只依据最终崩溃模块判断根因。

## 已确认的 AS346/ToLiss 规则

1. 跑道查询为：
   `SELECT ... FROM tbl_pg_runways WHERE runway_length > 4757`，没有 `ORDER BY`；WASM 随后却用只向前移动的机场游标处理结果。
2. 因此 `tbl_pg_runways` 必须按 `airport_identifier, runway_identifier` 的物理行顺序排列。新增跑道直接追加到表尾会导致新增机场无法进入机场索引。
3. 机场只有存在长度大于 4757 英尺的跑道时才会进入 AS346 的可检索机场集合。
4. AS346 固定内存加载器要求：
   - NDB `magnetic_variation` 不能为 NULL。
   - 航路点和航路标识不得超过目标缓冲区长度。
   - 航路固定字段不能为 NULL，距离必须在合理范围内。
   - SID、STAR 和 IAP 的非空 `waypoint_description_code` 必须恰好为 4 个字符；不足时右侧补空格。
   - `altitude_description` 只能是单字符 ARINC 约束。Fenix 的 `MAP` 是复飞点标记，不能写入该字段。
   - `waypoint_identifier`、`recommended_navaid` 与各自的 `*_icao_code` 必须保持 NULL 联动，不能出现标识符为空但 ICAO 代码非空的组合。
5. AS346 会在设置航线机场时加载该机场的完整 SID、STAR 和 IAP。2026-07-29 实机验证表明，违反上述程序字段契约的数据会在输入出发/到达机场后触发 WASM `0xc0000005`；规范化后 `ZUNZ/ZUUU` 的离场、进场和进近均可正常选择。其程序 SQL 不读取弧心字段，因此 `center_waypoint_icao_code` 的 NULL 联动属于一致性清理，不是此次崩溃的直接触发条件。
6. RF 航段必须至少具有正数 `arc_radius`。Fenix RF 原始行通常只提供弧心、上一航点、终点和转向，需要计算：
   - 半径：弧心到终点的大圆距离，单位海里。
   - 弧长：平均半径乘以按转向确定的圆心角，单位海里。
   - 可以同时写入 `route_distance_holding_distance_time='D'` 和 `distance_time`，但官方 AS346 数据表明后两者允许为空。
7. 程序转换必须按 Terminal、route type、transition 分段，并在段内重新生成连续 `seqno`。
8. Fenix 原始库可能包含除 ID 外完全相同的连续程序行，写入目标前必须去重，否则会生成零长度 RF 航段。
9. 公共程序段可能以 RF 开始。只有当前置跑道过渡段的终点完全一致时，才可继承该共享终点作为 RF 起点。
10. RF 缺失左右转向时，可以根据弧心到起终点的方位选择较短圆弧并补出转向；仍无法确定时不得输出无效 RF。
11. `RTE_SEG.csv` 的坐标是定长 DMS：纬度为 `DDMMSS`，经度为 `DDDMMSS`，必须根据 N/S/E/W 方向确定度数位数。不能仅按数字长度猜测，否则 `N271940` 会被误解析为 2719 度。
12. 构建 Fenix 航路时，缺失中间航点必须中断前后航点的距离计算，不能跳过缺失点后继续连接，否则会产生上千海里的假航段。
13. 合并官方航路与 `RTE_SEG` 时，应按航路标识、航点标识和坐标去重。`seqno` 只表示各数据源内部顺序，不能单独用于跨数据源判重。
14. 中国机场范围必须使用明确前缀 `ZB/ZG/ZH/ZJ/ZL/ZP/ZS/ZU/ZW/ZY`，不能使用 `LIKE 'Z%'`，否则会把朝鲜 `ZK` 和蒙古 `ZM` 的官方数据误判为中国补丁数据。
15. DFDv2 的机场 ICAO 区域码不一定等于机场标识前两位。2604 范例确认了 `ZBCF→ZY`、`ZUAL→ZW`、`ZUJZ→ZP`、`ZUPL→ZW` 等 21 个稳定例外；机场、跑道、终端航点和程序默认区域码必须使用同一映射。ILS 台站存在独立例外，不能无条件继承机场区域码。
16. RNP AR 的 `RWxx` PC 航点应以程序 MAP 坐标为准，并按 `region_code + waypoint_identifier` 更新已有记录。仅按坐标容差合并会在重复转换时留下旧坐标或插入重复点。

## 转换器设计要求

1. 每个目标机模建立独立 profile/adapter，至少包含：
   - schema 与版本识别。
   - 字段映射、单位换算和枚举映射。
   - NULL/default 规则和字符串长度。
   - 表级排序与索引要求。
   - 周期元数据和部署路径。
   - 目标专用验证器。
2. 转换必须确定性、可重复和尽量幂等。同一输入重复运行不得不断增加重复记录。
3. 区域补丁应按明确键替换中国区域记录，同时保留目标库的其他全球数据和机模专有字段。
4. 使用结构化 SQLite 操作，不用文本替换数据库内容。
5. 输出前以目标官方库做 schema diff；缺列、多列、列顺序变化或默认值差异必须明确处理。
6. 目标数据库存在多个版本时，通过可靠特征识别，不能仅依靠文件名。例如 AS346 可用 IAP 表的 `ctl` 字段识别 ToLiss 变体。

## 从 2607 生成所有机模数据

1. 先解析 `2607` 为不依赖机模的规范化中间模型，保留原始精度和来源字段。
2. 中间模型应覆盖机场、跑道、VHF/NDB、航路点、航路、ILS/GLS、SID/STAR/IAP、保持、通信、空域和 MORA。
3. 每个输出适配器只负责把中间模型投影到目标格式，不在适配器中重复解析原始文件。
4. 对无法无损表达的目标字段建立显式降级策略，并在转换报告中计数；禁止静默丢弃关键航段。
5. TFDI 和 FSL/FSLabs 开发前，先检查 `Fenix2TFDI-ref`、现有目标数据库以及机模实际加载程序，再确定格式。不要假定它们沿用 DFDv2。

## 验证顺序

每次生成候选数据至少执行以下层级：

1. SQLite `integrity_check`、journal mode、WAL/SHM 状态。
2. Header 与 `cycle.json` 等元数据一致性。
3. schema、行数、频率、坐标、字符串长度、NULL、枚举和距离范围。
4. 机场、跑道、程序和导航设施的引用完整性。
5. 使用机模真实 SQL 和游标逻辑模拟加载器。
6. 对新增机场、原有机场及边界机场进行点查，例如 `ZBCF`、`ZUNZ`、`ZUUU`。
7. 实机依次验证机场输入、设为出发/抵达、打开程序页、选择 SID/STAR/IAP、退出飞行和退出游戏。
8. 外部测试包必须包含中文说明、已知问题、安装/恢复方法、版本标识和 SHA-256，不得混入源码仓库。

## 测试与诊断

1. 修复缺陷前先构造最小失败测试或加载器模拟器，再修改实现。
2. 对参考官方库进行差分验证，重点比较同一路径终止类型和相同机场的字段分布。
3. 大型数据库不要加入单元测试。使用最小内存 SQLite fixture 覆盖已发现的真实契约。
4. 实机是最终判据，但在交给用户前必须尽量把失败条件转为本地确定性检查。
5. 每次实验只改变一个变量，并保留可恢复备份；不要用无法解释的 rowid 替换作为主要诊断方法。

## 部署路径与安全

- Community：`F:\games\community`。
- MSFS 2024 WASM：`C:\Users\Administrator\AppData\Roaming\Microsoft Flight Simulator 2024\WASM`。
- AS346 下载数据通常位于：
  `%APPDATA%\Microsoft Flight Simulator 2024\WASM\MSFS2024\aerosoft-aircraft-a346-pro\work\FMSData\cycle_XXXX`。
- 部署前检查 `FlightSimulator2024.exe` 不在运行。
- 同时备份数据库、`cycle.json` 和需要修改的 `layout.json`；部署后比较 SHA-256 并重新运行完整验证。

## Git 与发布

1. `.gitignore` 必须排除 `*.db3`、`*.s3db`、`*-wal`、`*-shm`、备份、诊断目录、反编译文件、WABT 工具和外部测试包。
2. 每个提交只包含一个可解释的修复或功能，并同时提交对应测试和文档。
3. 提交前检查暂存区，不能误提交用户数据库、游戏文件或无关修改。
4. 实机或约定的外部测试未通过时可以提交诊断分支，但不得发布正式版本；正式版本号和 GitHub Release 必须在验证通过后创建。
5. 正式 Release 必须上传由 `build_update_package.py` 生成的版本化 ZIP 资产，并保留 GitHub 提供的 SHA-256 digest，供一键更新执行双层完整性校验。
